<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--对应接口-->
<mapper namespace="com.example.mapper.OrderMapper">
    <select id="selectAllOrders1" resultType="com.example.entity.Order">
        select
        order_id as orderId, order_parent as orderParent, order_subject as orderSubject,
        order_grade as orderGrade, order_note as orderNote, teacher_name as teacherName,order_money as orderMoney
        from xm_order
    </select>

    <delete id="deleteById1">
        delete from xm_order where order_id = #{orderId}
    </delete>
</mapper>
